<UserControl
    x:Class="adaptive.viewemployee"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:adaptive"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:adaptive.Models"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
       
    <StackPanel>
        <TextBlock   Width="100" x:Name="one"  Text="secondpage"   Margin="10,10,10,10" />
        <TextBlock   Width="100" x:Name="two" Text="{x:Bind emp.firstname}"   Margin="10,10,10,10" />
        <TextBlock   Width="100" x:Name="three" Text="{x:Bind emp.lastname}"   Margin="10,10,10,10" />
        <TextBlock   Width="100" x:Name="four" Text="{x:Bind emp.id}"   Margin="10,10,10,10" />

        <TextBlock Width="100" x:Name="five" Text="{x:Bind emp.gender}" Margin="10,10,10,10"/>
        <TextBlock Width="100" x:Name="six" Text="{x:Bind emp.salary}" Margin="10,10,10,10"/>
        <TextBlock Width="150" x:Name="seven" Text="{x:Bind emp.phoneno}" Margin="10,10,10,10"/>
        <TextBlock Width="150" x:Name="eight" Text="{x:Bind emp.phoneno}" Margin="10,10,10,10"/>
        <TextBlock Width="120" x:Name="nine" Text="{x:Bind emp.dob}" Margin="10,10,10,10"/>
        <TextBlock Width="120" x:Name="ten" Text="{x:Bind emp.shift}" Margin="10,10,10,10"/>
    </StackPanel>
    
</UserControl>
